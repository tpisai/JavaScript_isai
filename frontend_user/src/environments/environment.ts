// Configuración para producción (ng build)
// Cambia apiUrl por la URL pública de tu API cuando la despliegues.
export const environment = {
  production: true,
  apiUrl: 'http://localhost:3000/api',
};
